<Project Sdk="Microsoft.NET.Sdk">
  <!--
    appB: Source-generated path (STUB for now)
    Part of Task #241: Dual-build infrastructure for parity testing.
    
    CURRENT: Identical to appA until new generator is implemented.
    FUTURE: Will use compile-time endpoint generation (zero-cost Build).
    
    Build: dotnet build
    Run:   dotnet run [args]
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>calc-appB</AssemblyName>
    <!-- Marker for future conditional compilation -->
    <DefineConstants>$(DefineConstants);NURU_GENERATED_PATH</DefineConstants>
  </PropertyGroup>

  <!-- Compile the shared source -->
  <ItemGroup>
    <Compile Include="../shared/calc.cs" Link="calc.cs" />
  </ItemGroup>

  <!-- 
    TODO: Replace with new source generator when implemented.
    For now, use same runtime path as appA.
  -->
  <ItemGroup>
    <ProjectReference Include="../../../../source/timewarp-nuru/timewarp-nuru.csproj" />
  </ItemGroup>

</Project>
